#include "include/equity/Block.h"

#include <gtest/gtest.h>

using namespace Equity;

TEST(EquityBlockTest, constuctor)
{
    GTEST_SKIP();
}

TEST(EquityBlockTest, constuctor_Header)
{
    GTEST_SKIP();
}

TEST(EquityBlockTest, constuctor_deserialize)
{
    GTEST_SKIP();
}

TEST(EquityBlockTest, serialize)
{
    GTEST_SKIP();
}

TEST(EquityBlockTest, toJson)
{
    GTEST_SKIP();
}

TEST(EquityBlockTest, header)
{
    GTEST_SKIP();
}

TEST(EquityBlockTest, transactions)
{
    GTEST_SKIP();
}

int main(int argc, char ** argv)
{
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
