#include "include/equity/Txid.h"

#include <gtest/gtest.h>

using namespace Equity;

TEST(EquityTxidTest, constructor)
{
    GTEST_SKIP();
}

TEST(EquityTxidTest, constructor_deserialization)
{
    GTEST_SKIP();
}

TEST(EquityTxidTest, serialize)
{
    GTEST_SKIP();
}

TEST(EquityTxidTest, toJson)
{
    GTEST_SKIP();
}

int main(int argc, char ** argv)
{
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
