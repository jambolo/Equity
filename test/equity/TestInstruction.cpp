#include "include/equity/Instruction.h"

#include <gtest/gtest.h>

using namespace Equity;

//    TEST(EquityInstructionTest, )
//    {
//    }

TEST(EquityInstructionTest, constructor)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, constructor_deserialization)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, serialize)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, toJson)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, op)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, data)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, location)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, size)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, valid)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, description)
{
    GTEST_SKIP();
}

TEST(EquityInstructionTest, getDescription)
{
    GTEST_SKIP();
}

int main(int argc, char ** argv)
{
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
